import GradientText from '@/components/ui/GradientText';

const Addresses = () => {
  return (
    <GradientText type="accent" text="Addresses" className="text-xl font-semibold sm:text-2xl" />
  );
};

export default Addresses;
