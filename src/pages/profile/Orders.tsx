import GradientText from '@/components/ui/GradientText';

const Orders = () => {
  return <GradientText type="accent" text="Orders" className="text-xl font-semibold sm:text-2xl" />;
};

export default Orders;
