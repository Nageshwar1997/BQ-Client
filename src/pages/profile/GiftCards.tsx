import GradientText from '@/components/ui/GradientText';

const GiftCards = () => {
  return (
    <GradientText type="accent" text="Gift Cards" className="text-xl font-semibold sm:text-2xl" />
  );
};

export default GiftCards;
