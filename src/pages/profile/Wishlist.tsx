import GradientText from '@/components/ui/GradientText';

const Wishlist = () => {
  return (
    <GradientText type="accent" text="Wishlist" className="text-xl font-semibold sm:text-2xl" />
  );
};

export default Wishlist;
