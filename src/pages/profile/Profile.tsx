import GradientText from '@/components/ui/GradientText';

const Profile = () => {
  return <GradientText type="accent" text="Profile" className="text-xl font-semibold sm:text-2xl" />;
};

export default Profile;
