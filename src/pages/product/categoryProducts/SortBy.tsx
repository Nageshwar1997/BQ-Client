const SortBy = ({ className = "" }: { className?: string }) => {
  return (
    <div className={`w-[400px] h-[500px] border border-[red] ${className}`}>
      SortBy
    </div>
  );
};

export default SortBy;
